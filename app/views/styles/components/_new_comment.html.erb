<%= simple_form_for [style, comment] do |f| %>
<%= f.input :content, as: :email, input_html: {class: 'comment-form'} %>
<%= f.button :submit, class: 'btn button-black border-black', id: 'comment-submit-button', disabled: true  %>
<% end %>
