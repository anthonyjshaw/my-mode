
<div class="container mt-4">
  <div class="row">
<h1>Men</h1>
</div>
<div class="row justify-content-center">
<% Item::ITEM_CATEGORIES.each do |category| %>
  <div class="card m-3 clothes-card" style="width: 18rem">
    <%= link_to "men/#{category}" do %>
     <%= image_tag "#{category}.jpg", alt: "alttext", class: 'clothes' %>
      <div class="card-title ">
        <p><%= category %></p>
      </div>
      <% end %>
    </div>
      <% end %>
  </div>
</div>
