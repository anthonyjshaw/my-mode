<div class="container my-4">
  <div class="row">
    <div class="col-sm-8">
      <article>
        <h1 class='mb-3'><%= @blog.title %></h1>
        <p><%= @blog.user.username %><span class="mx-3">Date <%= @blog.created_at.strftime("%d/%m/%Y") %></span></p>
        <hr>
        <section>
          <% @content.each do |line| %>
            <p><%= line %></p>
          <% end %>
        </section>
      </article>
    </div>
  </div>
</div>
